import React from 'react'
import ReactDOM from 'react-dom/client'
import ScreenCaptureEditor from './components/ScreenCaptureEditor'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <ScreenCaptureEditor />
    </React.StrictMode>
)

